info:
  name: Neuanlegen OAuth 2 user
  type: folder
  seq: 3

request:
  auth:
    type: oauth2
    flow: resource_owner_password_credentials
    accessTokenUrl: "{{oidcTokenUrl}}"
    credentials:
      clientId: "{{clientId}}"
      clientSecret: "{{clientSecret}}"
      placement: body
    resourceOwner:
      username: user
      password: p
    tokenConfig:
      id: credentials
      placement:
        header: Bearer
      source: access_token
    settings:
      autoFetchToken: true
      autoRefreshToken: false
  variables:
    - name: clientId
      value: javascript-client
    - name: clientSecret
      value: vDorIIwidFF8mjrgIWoLMR7mOY0mC1mr
