info:
  name: Anzahl
  type: http
  seq: 23

http:
  method: GET
  url: "{{restUrl}}?count-only=true"
  params:
    - name: count-only
      value: "true"
      type: query
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
