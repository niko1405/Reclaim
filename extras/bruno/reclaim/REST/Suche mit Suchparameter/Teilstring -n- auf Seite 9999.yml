info:
  name: Teilstring "n" auf Seite 9999
  type: http
  seq: 5

http:
  method: GET
  url: "{{restUrl}}?titel=n&page=9999"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: titel
      value: n
      type: query
    - name: page
      value: "9999"
      type: query
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
