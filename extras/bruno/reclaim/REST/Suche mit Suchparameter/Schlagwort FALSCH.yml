info:
  name: Schlagwort FALSCH
  type: http
  seq: 13

http:
  method: GET
  url: "{{restUrl}}?FALSCH=true"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: FALSCH
      value: "true"
      type: query
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
