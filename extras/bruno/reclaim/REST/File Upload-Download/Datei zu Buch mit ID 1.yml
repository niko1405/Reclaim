info:
  name: Datei zu Buch mit ID 1
  type: http
  seq: 3

http:
  method: GET
  url: "{{restUrl}}/file/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
