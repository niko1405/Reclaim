info:
  name: Datei zu AppProfile
  type: http
  seq: 2

http:
  method: GET
  url: "{{restUrl}}/file/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
