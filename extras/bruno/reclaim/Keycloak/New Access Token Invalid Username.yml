info:
  name: New Access Token Invalid Username
  type: http
  seq: 5

http:
  method: POST
  url: "{{oidcTokenUrl}}"
  body:
    type: form-urlencoded
    data:
      - name: grant_type
        value: password
      - name: username
        value: FALSCH
      - name: password
        value: p
  auth:
    type: basic
    username: "{{clientId}}"
    password: "{{clientSecret}}"

runtime:
  variables:
    - name: clientId
      value: javascript-client
    - name: clientSecret
      value: vDorIIwidFF8mjrgIWoLMR7mOY0mC1mr
  assertions:
    - expression: res.status
      operator: eq
      value: "400"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
