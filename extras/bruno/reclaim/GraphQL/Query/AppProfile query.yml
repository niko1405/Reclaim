info:
  name: AppProfile query
  type: graphql
  seq: 1

graphql:
  method: POST
  url: "{{graphqlUrl}}"
  headers:
    - name: Accept
      value: application/graphql-response+json
  body:
    query: |-
      query FindProfilesByFilter($input: SuchParameterInput) {
        appProfiles(input: $input) {
          id
          displayName
          currentStreak
        }
      }
    variables: |-
      {
        "input": {
          "displayName": "n"
        }
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.headers['content-type']
      operator: matches
      value: ^application\/graphql-response\+json(; charset=utf-8)?$

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
